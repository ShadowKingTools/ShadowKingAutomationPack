# PerformanceProfile.ps1
# ShadowKingAutomationPack - starter script

Write-Host "PerformanceProfile: starting..." -ForegroundColor Cyan

# TODO: later we'll add real tweaks (power plan, game mode reminders, etc.)
# Keep this safe + simple for now.

Write-Host "PerformanceProfile: done (no system changes yet)." -ForegroundColor Yellow
exit 0
